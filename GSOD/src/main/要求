全球站点分布显示：
    当前项目的问题：
        1):某些站点存在没有经纬度坐标（删除）
        2):某些站点存在相同的经纬度坐标（判断每个站点的数据量，保留数据量最大的，删除其余的）

    MapReduce程序，如果指定的输入文件为 .op.gz 结尾的压缩文件， MapReduce可以自行解压，正常获取数据
    MapRedece程序，可以指定输入文件目录为，但此时要求该目录下不能出现子目录，不然会报错

    要求：
    1)要求处理的气象数据指标不少于两个
    2)将gsod_2020.tar文件中不存在站点从stations表中删除
        将stations表中不存在的站点从gsod_2020.tar中删除
        得到gsod_2020.tar文件中的站点中和stations表中站点是一一对应


每个小组：
    准备一个演讲的PPT：简洁，美观，大方（尽量不要大篇幅的字）
    准备演讲的人（主持人）

    每个小组10-15分钟，小组介绍，项目介绍，小组人员分工，项目演示，项目答辩
        项目答辩（根据每个人分工的不同，进行技术答辩）

每个小组最后需要提交的东西：演讲的PPT，项目（源码），每个人的项目（源码），每个的实验报告（个人任务实现，思路）