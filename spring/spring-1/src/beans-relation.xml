<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:p="http://www.springframework.org/schema/p"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd">

	<!-- 抽象bean: bean 的abstract 属性为true 的bean 不能被IOC 容器实例化，只用来被继承配置,
	若某一个bean 的class 属性没有指定, 则该bean 必须是一个抽象bean -->
	<bean id="address" 
		p:city="Beijing^" p:street="WuDaKou" abstract="true"></bean>
	
	<!-- type parent extends address -->
	<bean id="address2" class="com.spring.beans.autowire.Address"
		parent="address"></bean>
		
	<bean id="address3" class="com.spring.beans.autowire.Address"
		parent="address" p:street="DaZhongSi"></bean>

	<bean id="car" class="com.spring.beans.autowire.Car"
		p:brand="Audi" p:price="300000"></bean>

	<!-- 要求配置Person 时, 必须有一个关联的car! 换句话说person 这个bean 依赖于Car 这个bean -->
	<bean id="person" class="com.spring.beans.autowire.Person"
		p:name="Tom" p:address-ref="address2" depends-on="car" p:car-ref="car"></bean>

</beans>
